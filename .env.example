# Local dev: use /api (Vite proxies to localhost:5000)
# Production (Render/Netlify): use your backend URL, e.g. https://your-api.onrender.com/api
VITE_API_URL=/api

# Optional — only if you want Google sign-in
VITE_GOOGLE_CLIENT_ID=
